DELIBE-326: Build with setuptools and a `setup.py` shim so the package can be a `zc.buildout` develop egg, since `develop` runs `setup.py` directly and cannot handle a PEP 517 only package. Grant the Manager role with `plone.app.testing.setRoles` instead of `@pytest.mark.portal(roles=...)`, which is a no-op on the `pytest-plone` that the Plone 6.1 constraints resolve. Bound `requires-python` to `<3.14`, which the classifiers and the CI matrix already stopped at.
